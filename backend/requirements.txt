# ── Aeroinsights Backend — Runtime requirements ───────────────────────────────
# Dev image: excludes weasyprint (native GTK/Cairo), scipy (Phase-2 ECL math),
# boto3 (S3), postmarker (email) — none are imported in Phase-1 code.
# Add them to requirements-full.txt for production/CI builds.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# Auth
python-jose[cryptography]>=3.3.0
httpx>=0.28.0

# Validation / settings
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0

# Task queue
celery[redis]>=5.4.0
redis>=5.2.0

# File handling
openpyxl>=3.1.5
python-multipart>=0.0.18
pandas>=2.2.0

# Observability
structlog>=24.4.0
