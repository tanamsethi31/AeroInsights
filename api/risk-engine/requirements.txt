numpy>=1.26,<3.0
# Pin StrEnum to a release whose wheel includes the .dist-info/INSTALLER
# metadata file. 0.4.15 ships without it, causing Vercel's deploy step
# to fail on lstat. Force-resolved here because supabase-py pulls it
# transitively via gotrue / realtime.
StrEnum==0.4.10
supabase>=2.7,<3.0
